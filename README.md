# intl-typegen

## Development Dependencies

This project includes both `@typescript/native-preview` and `typescript`:

- `@typescript/native-preview` - Used for type checking
- `typescript` - Required as a peer dependency by `tsup`